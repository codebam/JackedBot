/// <reference types="@cloudflare/vitest-pool-workers/types" />
// Pulls in `cloudflare:test` module declarations plus the Workers runtime globals
// for the workerd test project. The main project's `Env` comes from
// worker-configuration.d.ts (npm run types).
